import { FileBrowser } from '@jupyterlab/filebrowser';
import { IGitExtension } from '../tokens';
export declare function addCloneButton(model: IGitExtension, filebrowser: FileBrowser): void;
/**
 * Callback method on Git Clone button in the File Browser toolbar.
 * 1. Invokes a new dialog box with form fields.
 * 2. Invokes the server API with the form input.
 */
export declare function doGitClone(model: IGitExtension, path: string): Promise<void>;
