/// <reference types="react" />
import { ReactWidget } from '@jupyterlab/apputils';
import { IRenderMimeRegistry } from '@jupyterlab/rendermime';
import { Widget } from '@lumino/widgets';
import { GitExtension } from '../model';
import { ISettingRegistry } from '@jupyterlab/settingregistry';
import { FileBrowserModel } from '@jupyterlab/filebrowser';
/**
 * A class that exposes the git plugin Widget.
 */
export declare class GitWidget extends ReactWidget {
    constructor(model: GitExtension, settings: ISettingRegistry.ISettings, renderMime: IRenderMimeRegistry, filebrowser: FileBrowserModel, options?: Widget.IOptions);
    render(): JSX.Element;
    private _model;
    private _renderMime;
    private _settings;
    private _filebrowser;
}
